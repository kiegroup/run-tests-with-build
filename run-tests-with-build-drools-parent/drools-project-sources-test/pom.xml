<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <artifactId>run-tests-with-build-drools-parent</artifactId>
    <groupId>org.kie</groupId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <packaging>pom</packaging>

  <artifactId>drools-project-sources-test</artifactId>

  <properties>
    <filter1>drools-examples-api/kiemodulemodel-example</filter1>
    <filter2>drools-examples-api/kie-module-from-multiple-files</filter2>
    <filter3>drools-examples-api/named-kiesession-from-file</filter3>
    <filter4>drools-examples-api/kiecontainer-from-kierepo</filter4>
    <filter5>drools-examples-api/default-kiesession-from-file</filter5>
<!--    <filter6>kie-dmn/kie-dmn-core</filter6>-->
    <filter7>drools-test-coverage/standalone/kie-ci-without-domain/tests</filter7>
    <filter8>drools-test-coverage/standalone/kie-ci-with-domain/tests</filter8>
    <filter9>kie-archetypes</filter9>
    <filter10>kie-archetypes/kie-drools-archetype</filter10>
    <filter11>kie-archetypes/kie-kjar-archetype</filter11>
    <filter12>kie-archetypes/kie-model-archetype</filter12>
    <filter13>kie-archetypes/kie-service-spring-boot-archetype</filter13>

    <maven.modules.explicit.filtering>${filter1},${filter2},${filter3},${filter5},${filter6},${filter7},${filter8},${filter9},${filter10},${filter11},${filter12},${filter13}</maven.modules.explicit.filtering>
    <maven.modules.explicit.filtering.type>out</maven.modules.explicit.filtering.type>

    <project1>!drools-examples</project1>
    <maven.additional.options>-pl ${project1}</maven.additional.options>
  </properties>

  <dependencies>
    <!-- define as dependency to assure reactor order -->
    <dependency>
      <groupId>org.kie</groupId>
      <artifactId>run-tests-with-build-project-sources-dependency-get</artifactId>
      <type>pom</type>
      <version>${project.version}</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-invoker-properties</id>
          </execution>
        </executions>
      </plugin>
      <!-- Invoking groovy script that dynamically decides if project should be included in build or not -->
      <plugin>
        <groupId>org.codehaus.gmavenplus</groupId>
        <artifactId>gmavenplus-plugin</artifactId>
        <executions>
          <execution>
            <id>resolve-includes</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>execute</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-invoker-plugin</artifactId>
        <configuration>
          <parallelThreads>1</parallelThreads>
          <!--
          script deciding if included project is:
            executed (either script missing or returning true)
              or
            skipped (when returns false or throws error) -->
          <selectorScript>${maven.modules.resolution.selector.script.name}</selectorScript>
          <pomIncludes>
            <!-- includes everything, filtering is done in resolve-includes.groovy script
            which adds invoker-run.groovy script containing 'return false' near each pom.xml
            that should be excluded.-->
            <pomInclude>**/pom.xml</pomInclude>
<!--            <pomInclude>**/kie-dmn/kie-dmn-core/pom.xml</pomInclude>-->
          </pomIncludes>
          <pomExcludes>
            <pomExclude>**/kie-archetypes/kie-drools-archetype/pom.xml</pomExclude>
            <pomExclude>**/kie-archetypes/kie-drools-archetype/src/main/resources/archetype-resources/pom.xml</pomExclude>
            <pomExclude>**/kie-archetypes/kie-service-spring-boot-archetype/src/main/resources/archetype-resources/pom.xml</pomExclude>
            <pomExclude>**/drools-test-coverage/standalone/kie-ci-with-domain/tests/pom.xml</pomExclude>
            <pomExclude>**/drools-test-coverage/standalone/kie-ci-without-domain/tests/pom.xml</pomExclude>
            <!-- Excludes work still as expected. -->
          </pomExcludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>